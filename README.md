# TurboFox

A replica of terminal.shop in the browser.

## Usage

Prepare the environment file:

```
cp .env.example .env
```

Prepare the application:

```
uv run inv all
```

Run the application:

```
uv sync
uv run inv dev
```

